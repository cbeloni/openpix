name: Deploy production

on:
  push:
    branches: [ main ]

jobs:
  meu-job:
    runs-on: ubuntu-latest
    environment:
      name: open-pix
    steps:
     - name: Run Remote Script
       run: echo ${{ secrets.SSH_PRIVATE_KEY }} & ssh ubuntu@177.93.128.55 /home/ubuntu/api/deploy-openpix/start-server.sh
       env: 
         SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
